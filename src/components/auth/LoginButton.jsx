// src/components/auth/LoginButton.jsx
import React from 'react';
import { useAuth } from '@/contexts/AuthContext';

// Ensure the function name matches the export name
export function LoginButton() { 
    // ... code ...
}
